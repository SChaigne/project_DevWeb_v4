{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h2>{{article.idCrypto.name}}</h2>
    <h1>{{article.titre}}</h1>
    <p>{{article.content}}</p>
    <p>Auteur : {{article.idUser.login}}</p>
    <p>{{article.date ? article.date|date('Y-m-d H:i:s') : '' }}</p>

    <h1>Commentaires</h1>
    {% for commentary in article.commentaries %}
        <p>{{commentary.idUser.login}} : {{commentary.text}}</p>     
    {% endfor %}

    {{ include('commentary/_form.html.twig') }}

    {#<table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article.content }}</td>
            </tr>
            <tr>
                <th>NbLike</th>
                <td>{{ article.nbLike }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ article.date ? article.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_index') }}">back to list</a>

    <a href="{{ path('app_article_edit', {'id': article.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }}
    #}
{% endblock %}
