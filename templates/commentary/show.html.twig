<div class="card w-50 m-4">
  <div class="card-header">
    {{commentary.idUser.login}} {#le {{commentary.date ? commentary.date|date('Y-m-d H:i:s') : '' }} #}
  </div>
  <div class="card-body">
    <p class="card-text">{{commentary.text}}</p>
  </div>
</div>





    {#<h1>Commentary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentary.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ commentary.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentary_index') }}">back to list</a>

    <a href="{{ path('app_commentary_edit', {'id': commentary.id}) }}">edit</a>

    {{ include('commentary/_delete_form.html.twig') }}
    #}
